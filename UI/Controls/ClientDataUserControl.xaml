<UserControl x:Class="UI.Controls.ClientDataUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:Presentation.ViewModels;assembly=Presentation"
             xmlns:controls = "clr-namespace:UI.Controls"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance viewmodels:ClientListViewModel}">
    <Grid Background="#FFE5E5E5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ItemsControl Grid.Column="0" ItemsSource="{Binding Client}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <controls:ClientUserControl Height="Auto" Width="Auto" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="Add Client" Command="{Binding ShowNewClientControl}"  Grid.Row="1" Grid.Column="1" />
    </Grid>
</UserControl>